import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="app-header">
      <div className="app-header__title">To-do List</div>
      <div className="app-header__subtitle">Plan, organize, and track your tasks</div>
    </header>
  );
};

export default Header;
